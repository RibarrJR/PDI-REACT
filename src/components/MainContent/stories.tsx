import { Story, Meta } from '@storybook/react'
import MainContent from '.'

export default {
  title: 'MainContent',
  component: MainContent
} as Meta

export const Default: Story = () => <MainContent />
